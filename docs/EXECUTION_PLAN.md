# Execution Plan

1. Implement cover prompt resolution from module-level config and module directory files, preserving existing precedence and defaults.
2. Add cover generation execution flow with dry-run support and injectable provider (HTTP endpoint or script), keep code modular for tests.
3. Update CLI docs and add tests for cover prompt selection and dry-run behavior.
